import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'com.phuc1nguyen.budget',
	appName: 'budget',
	webDir: 'build',
	server: {
		androidScheme: 'build'
	}
};

export default config;
